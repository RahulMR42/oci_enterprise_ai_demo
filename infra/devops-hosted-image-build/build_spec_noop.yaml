version: 0.1
component: build
timeoutInSeconds: 300
shell: bash
failImmediatelyOnError: true

steps:
  - type: Command
    name: legacy-placeholder
    timeoutInSeconds: 300
    command: |
      set -euo pipefail
      echo "Legacy build stage retained to avoid OCI DevOps predecessor deletion conflicts."
